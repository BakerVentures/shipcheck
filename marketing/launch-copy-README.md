# Before you post any of this

`show-hn.md`, `x-thread.md`, and `product-hunt.md` came from the original
marketing kit drop, not from me. Wording is consistent with what actually
shipped (pricing, "free scan on every app" not "first scan," and the assets
referenced are the real ones in `docs/demo/` and `docs/assets/ph-gallery/`).

**Resolved:** all three originally opened with a first-person rejection story
("I got rejected three times...") that I couldn't verify happened to you, and
flagged that clearly rather than silently leaving it or inventing a different
fake one. I've since replaced it in all three with a real, sourced story: the
fake-discount-copy paywall rejection documented in RizzMaxx's own
`docs/build-29-conversion-growth-plan.md:60` ("Fake-discount copy got us
rejected once; the price on the button must be real") -- a genuine paywall-
copy rejection, and a close match to shipcheck's actual 3.1.2 checks (paywall
discloses accurate terms).

If that framing doesn't match how you'd tell the story yourself, or if you'd
rather use a different real incident, edit the three files directly -- the
anecdote is now a short, single sentence in each (easy to swap), not the
three-part structured story the original kit drafted.

Also worth knowing: `flirtgym-1.0.3-cowork-prompt.md` has a full
pre-submission checklist for exactly the things shipcheck checks (renewal
terms in a sticky footer, Restore Purchases, dead legal links, account
deletion reachable without an active subscription) with the comment "this is
the App Store 3.1.2 rejection surface" -- real evidence the team has hardened
against this from hard experience, if you want more material for a longer
post.
